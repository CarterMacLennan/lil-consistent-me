<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lilconsistentme">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".GuideActivity"></activity>
        <activity android:name=".MainActivity" android:screenOrientation="nosensor">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RightActivity" android:screenOrientation="nosensor"/>
        <activity android:name=".LeftActivity" android:screenOrientation="nosensor"/>
        <activity android:name=".DefineActivity" android:screenOrientation="nosensor"/>
        <activity android:name=".FrequencyActivity" android:screenOrientation="nosensor"/>
        <activity android:name=".DiedActivity" android:screenOrientation="nosensor"/>
    </application>

</manifest>